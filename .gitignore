# --------------------------------------------------------------------
# 1. Python 필수 (컴파일된 바이너리 및 캐시)
# --------------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class

# --------------------------------------------------------------------
# 2. 가상환경 (Virtual Environments)
# - venv, env, .venv 등 통상적으로 사용하는 폴더명 모두 포함
# --------------------------------------------------------------------
venv/
env/
.venv/
.env_*/
ENV/

# --------------------------------------------------------------------
# 3. 환경 변수 및 보안 중요 파일 (절대 커밋 금지)
# - 디스코드 봇 토큰 등이 들어있는 파일
# --------------------------------------------------------------------
.env
.env.local
.env.*.local
config_private.py
secrets.json

# --------------------------------------------------------------------
# 4. 배포 및 빌드 관련 (라이브러리 설치 부산물)
# --------------------------------------------------------------------
dist/
build/
*.egg-info/
.eggs/
parts/
var/
sdist/
develop-eggs/
wheels/
pip-wheel-metadata/

# --------------------------------------------------------------------
# 5. IDE 및 에디터 설정 파일 (개인 설정이므로 공유 X)
# --------------------------------------------------------------------
# VS Code
.vscode/
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.code-workspace

# PyCharm (JetBrains)
.idea/

# --------------------------------------------------------------------
# 6. 로그 및 데이터베이스
# --------------------------------------------------------------------
*.log
*.sqlite3
*.db

# --------------------------------------------------------------------
# 7. 운영체제 생성 파일
# --------------------------------------------------------------------
.DS_Store
Thumbs.db